alias usage='du -sh -- * | sort -hr'
alias master_rebase='CURRENT_BRANCH=$(git symbolic-ref --short -q HEAD) && git checkout master && git pull && git checkout $CURRENT_BRANCH && git rebase master'
alias mtr='sudo mtr'
